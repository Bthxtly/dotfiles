#include <iostream>
using namespace std;

template <typename T>
T Abs(T x) {
    return x > 0 ? x : -x;
}

int main()
{
    int a, b;
    cin >> a;
    b = Abs(a);
    cout << b << endl;
    return 0;
}
