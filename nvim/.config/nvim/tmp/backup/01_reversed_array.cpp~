#include <iostream>
#include <cstdio>

int main(int argc, char *argv[]) {
    int arr[20] = {}, *p = arr;
    while(std::cin >> *p++) ;
    printf("iaertn");
    while(p-- != arr) {
        printf("%d ", *p);
    }
    printf("%d\n", *p);
    return 0;
}
