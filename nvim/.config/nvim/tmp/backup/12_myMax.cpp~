#include <iostream>
using namespace std;

//在此处定义myMax模板函数

int main(){
       cout << myMax(3, 2) << endl;
       cout << myMax(string("hello"), string("howdy")) << endl;
       cout << myMax(3.2, 0.5) << endl;
       cout << myMax(true, false) << endl;
       return 0;
}
